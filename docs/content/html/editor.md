# HTML 编辑器

使用 NotePad 或 TextEdit 来编写 HTML。

可以使用专业的 HTML 编辑器来编辑 HTML，但是我们同时推荐使用文本编辑器 (Sublime Text, VS Code 等) 来进行 HTML 的编写。

## 步骤 1: 新建 HTML 文件

在 VS Code 中新建一个文件，后缀名为 `.html`。

## 步骤 2: 编写 HTML 代码

```html
<!DOCTYPE html>
<html>
<body>

<h1>我的第一个标题</h1>

<p>我的第一个段落。</p>

</body>
</html>
```
